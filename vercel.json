{
  "buildCommand": "git submodule update --init --recursive && cd web && npm install && npm run build",
  "outputDirectory": "web/.next",
  "installCommand": "git submodule update --init --recursive && cd web && npm install",
  "framework": "nextjs",
  "rootDirectory": "web"
}
